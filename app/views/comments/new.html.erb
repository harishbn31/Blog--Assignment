<h2>Add comment</h2>

<%= form_for @comment do |f| %>

	<label>Body :</label><br/>
	<%= f.text_area :body %>
	<%= f.hidden_field :user_id, value: @user_id%>
	<%= f.hidden_field :article_id, value: @article_id%>

	<%= f.submit %>

<% end %>